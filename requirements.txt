# ARA OS - CC version - Desktop 4.0
# Tottenham A.C. - Official Requirements
# Python 3.6+ required
# Install with: pip install -r requirements.txt
# Core dependencies
flask>=2.0.1
pywebview>=3.6
psutil>=5.8.0
gputil>=1.4.0
chatterbot>=1.0.8
chatterbot-corpus>=1.2.0
googletrans>=3.1.0a0

# Dependencies já incluídas na stdlib (não precisam ser instaladas)
# threading, time, os, json, uuid, platform, socket, datetime, random

# Dependencies do Flask (instaladas automaticamente com flask)
# request, jsonify

# Dependencies do ChatterBot
spacy>=2.3.5
nltk>=3.5
sqlalchemy>=1.3

# Dependencies opcionais para melhor desempenho
python-Levenshtein>=0.12.2

